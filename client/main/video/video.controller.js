(function () {
    angular.module('crossapp')
        .controller('VideoController', function ($scope, $localStorage, $state,
                                                $rootScope, toastr, $ocLazyLoad) {
            var self = this;

        });
})();
